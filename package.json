{
  "name": "integrate-epns.js",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "rm -rf .cache dist && parcel example/index.html --port 3600 --no-autoinstall",
    "build": "rm -rf .cache dist && parcel build example/index.html --no-source-maps --no-autoinstall"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^14.14.12",
    "parcel-bundler": "^1.12.4",
    "parcel-plugin-inliner": "^1.0.14",
    "typescript": "^4.1.3"
  },
  "dependencies": {
    "ethers": "^5.0.24"
  }
}
